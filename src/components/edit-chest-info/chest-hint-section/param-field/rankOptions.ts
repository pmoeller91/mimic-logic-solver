import { CHEST_RANK, ChestRank } from "@/types/chestHint";

const rankOptions: ChestRank[] = [
  CHEST_RANK.topMost,
  CHEST_RANK.rightMost,
  CHEST_RANK.bottomMost,
  CHEST_RANK.leftMost,
];

export { rankOptions };
